/**
 * @file az_contact.js
 * Manage Contact related JavaScript
 */
(function ($) {
  'use strict';

  /*
  if (!Drupal.az) {
    Drupal.az = {};
  }

  Drupal.az.contactC = function () {

    var $container;

    return {
      initBlocks: initBlocks,
      initTabs: initTabs
    };
  };

  Drupal.behaviors.az_contact = {
    // Attach functions are executed by Drupal upon page load or ajax loads.
    attach: function (context, settings) {
      if (!Drupal.az.contact) {  // Ensures we only run this once
        Drupal.az.contact = Drupal.az.contactC();
      }
      $('.az-tabs').once('az-attached').each(function () {
        Drupal.az.contact.initTabs($(this), context);
      });
      $('.contact-block').once('az-attached').each(function () {
        Drupal.az.contact.initBlocks($(this), context);
      });
    }
  };
  */

}(jQuery));

